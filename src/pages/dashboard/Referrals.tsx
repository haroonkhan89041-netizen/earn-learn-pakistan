import { useEffect, useState } from 'react';
import toast from 'react-hot-toast';
import { Copy, Users, ShieldAlert, Sparkles, ArrowUpRight } from 'lucide-react';
import { useAuth } from '@/contexts/AuthContext';
import { supabase, isSupabaseConfigured } from '@/lib/supabase';
export function Referrals(){const{profile}=useAuth();const code=profile?.referral_code??'';const link=code?`${window.location.origin}/signup?ref=${encodeURIComponent(code)}`:'';const[copied,setCopied]=useState(false);const[history,setHistory]=useState<Array<{id:string;name:string;status:string;reward:number;date:string}>>([]);const[counts,setCounts]=useState({total:0,active:0,rewardPoints:0});useEffect(()=>{if(!isSupabaseConfigured||!profile)return;(async()=>{const{data,error}=await supabase.from('referrals').select('id,status,reward_points,created_at,referred_user_id').eq('referrer_id',profile.id).order('created_at',{ascending:false});if(error||!data)return;const ids=data.map(r=>r.referred_user_id);const{data:people}=ids.length?await supabase.from('profiles').select('id,full_name').in('id',ids):{data:[]};const names=new Map((people??[]).map(p=>[p.id,p.full_name||'New user']));const rows=data.map(r=>({id:r.id,name:names.get(r.referred_user_id)||'New user',status:r.status,reward:r.reward_points,date:new Date(r.created_at).toLocaleDateString()}));setHistory(rows);setCounts({total:rows.length,active:rows.filter(r=>r.status==='qualified'||r.status==='rewarded').length,rewardPoints:rows.reduce((sum,r)=>sum+r.reward,0)});})();},[profile]);function copy(){if(!link)return;navigator.clipboard.writeText(link);setCopied(true);toast.success('Referral link copied');setTimeout(()=>setCopied(false),1500);}return <div className="space-y-6"><div><div className="mb-2 inline-flex items-center gap-2 rounded-full border border-brand-blue/20 bg-brand-blue/5 px-3 py-1 text-xs font-semibold text-brand-blue"><Sparkles size={13}/>Referral program</div><h1 className="font-display text-3xl font-extrabold tracking-tight text-navy-900">Referrals</h1><p className="mt-1 text-sm text-navy-500">Invite friends, grow the community, and earn points when they qualify.</p></div><div className="card overflow-hidden"><div className="bg-gradient-to-r from-brand-blue/10 via-brand-indigo/5 to-transparent p-6"><p className="text-xs font-semibold uppercase tracking-wider text-brand-blue">Your referral link</p><div className="mt-3 flex flex-col gap-3 sm:flex-row"><input readOnly value={link||'Referral code is not available yet'} className="input flex-1 font-mono text-xs"/><button className="btn-primary shrink-0" onClick={copy} disabled={!link}><Copy size={15}/>{copied?'Copied!':'Copy link'}</button></div><p className="mt-3 text-sm text-navy-500">Code: <span className="font-mono font-semibold text-navy-900">{code||'—'}</span></p></div></div><div className="grid gap-4 sm:grid-cols-3"><div className="card p-5"><p className="text-xs font-semibold uppercase tracking-wider text-navy-500">Total referrals</p><p className="mt-2 font-mono text-3xl font-bold text-navy-900">{counts.total}</p></div><div className="card p-5"><p className="text-xs font-semibold uppercase tracking-wider text-navy-500">Qualified referrals</p><p className="mt-2 font-mono text-3xl font-bold text-navy-900">{counts.active}</p></div><div className="card p-5"><p className="text-xs font-semibold uppercase tracking-wider text-navy-500">Referral rewards</p><p className="mt-2 font-mono text-3xl font-bold text-brand-green-dark">{counts.rewardPoints} pts</p></div></div><div className="card overflow-hidden"><div className="border-b border-navy-100 p-5"><h2 className="font-display text-base font-bold">Referral history</h2><p className="mt-1 text-xs text-navy-400">See who joined through your link and the rewards earned.</p></div><div className="divide-y divide-navy-100">{history.length===0?<p className="p-5 text-sm text-navy-400">No referrals yet.</p>:history.map(h=><div key={h.id} className="flex items-center justify-between gap-4 px-5 py-4"><div><p className="text-sm font-semibold text-navy-800">{h.name}</p><p className="mt-1 text-xs text-navy-400">{h.date}</p></div><div className="flex items-center gap-3"><span className="badge bg-navy-100 text-navy-500 capitalize">{h.status}</span><span className="font-mono text-sm font-semibold text-navy-700">{h.reward?`+${h.reward}`:'—'}</span></div></div>)}</div></div><div className="card flex items-start gap-3 border-brand-amber/30 bg-brand-amber/5 p-4"><ShieldAlert size={18} className="mt-0.5 shrink-0 text-amber-600"/><p className="text-xs text-amber-800">Self-referrals and duplicate-account abuse can be reviewed by an administrator.</p><ArrowUpRight size={14} className="ml-auto shrink-0 text-amber-600"/></div></div>}
